(function (require, __filename, __dirname, module) {
    module.exports = {};
    module.exports = exports;

    /*
     * 2-module 对象
     * 该对象包含了该模块本身所有的信息
     * 2-module.id 显示模块id 也是模块地址
     * 2-module.children 返回数组 该模块被哪些模块依赖，就返回这些模块
     * 2-module.parent   返回对象 该模块依赖了哪些模块
     * 2-module.filename 返回该模块的路径
     * 2-module.loaded   该模块是否被加载
     *
     * */

    /*
    * require(); 该方法的细节请参考导图
    *
    * */

    /*
    * 注释外部的函数模型就是一个模块，隐藏的信息
    *
    * */

    return module.exports;

});